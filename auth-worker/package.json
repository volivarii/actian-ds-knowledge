{
  "name": "actian-ds-knowledge-auth-worker",
  "version": "0.1.0",
  "private": true,
  "description": "OAuth broker for the Knowledge Editor — exchanges GitHub OAuth codes for user-to-server tokens.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
