{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://actian-ds.example.com/schemas/app-context-app.json",
  "title": "App-context app (per-record frontmatter)",
  "description": "Frontmatter of app-context/src/apps/<slug>.md. Describes an Actian application's identity, header variant, navigation structure, and behavioural signals.",
  "type": "object",
  "required": ["_schema_version", "slug", "label", "header", "sidebar"],
  "additionalProperties": false,
  "properties": {
    "_schema_version": { "const": 1 },
    "slug": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "maxLength": 60,
      "description": "Kebab-case identifier for the app; used as the filename (minus .md) and cross-reference target.",
      "examples": ["studio", "explorer", "data-catalog"]
    },
    "label": {
      "type": "string",
      "description": "Human-readable display name of the app shown in navigation and documentation.",
      "examples": ["Studio", "Explorer", "Data Catalog"]
    },
    "header": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "description": "Global-header configuration for the app; controls the DS header variant rendered at runtime.",
      "examples": [{ "type": "Studio" }],
      "properties": {
        "type": {
          "type": "string",
          "minLength": 1,
          "description": "Header variant; matches a DS global-header App type. Open by design so new apps can declare their own header.",
          "examples": ["Studio", "Explorer", "Admin", "Observability"]
        }
      }
    },
    "sidebar": {
      "type": "array",
      "description": "Ordered list of primary navigation items shown in the app sidebar.",
      "examples": [
        [
          { "label": "Pipelines", "id": "pipelines" },
          { "label": "Connections", "id": "connections" }
        ]
      ],
      "items": {
        "type": "object",
        "required": ["label", "id"],
        "additionalProperties": false,
        "properties": {
          "label": {
            "type": "string",
            "description": "Display text for the sidebar navigation item.",
            "examples": ["Pipelines", "Connections", "Settings"]
          },
          "id": {
            "type": "string",
            "description": "Stable identifier for the sidebar item; used for deep-linking and analytics.",
            "examples": ["pipelines", "connections", "settings"]
          }
        }
      }
    },
    "useCases": {
      "type": "array",
      "description": "What the app is FOR, framed as audience → jobs. Optionally links each use case to app-scoped patterns. Drives flow framing + scaffold selection in consumers.",
      "examples": [
        [
          {
            "audience": ["Data steward", "Data engineer"],
            "jobs": ["Govern the catalog", "Review access requests"],
            "patterns": ["asset-detail-360"]
          }
        ]
      ],
      "items": {
        "type": "object",
        "required": ["audience", "jobs"],
        "additionalProperties": false,
        "properties": {
          "audience": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string" },
            "description": "Primary users this use case serves.",
            "examples": [["Data steward", "Data engineer"]]
          },
          "jobs": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string" },
            "description": "Jobs-to-be-done for this audience in this app.",
            "examples": [["Govern the catalog", "Review access requests"]]
          },
          "patterns": {
            "type": "array",
            "items": { "type": "string" },
            "description": "App-scoped pattern slugs idiomatic to this use case. Integrity (exists + app-scoped) enforced by validate-app-context.js.",
            "examples": [["asset-detail-360", "access-request-management"]]
          }
        }
      }
    }
  }
}
