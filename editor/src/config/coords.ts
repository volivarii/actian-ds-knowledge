// Canonical GitHub coordinates the editor reads from and writes back to.
// Centralized here so the Actian-org transfer is a single-file change.
export const DEFAULT_COORDS = {
  owner: "volivarii",
  repo: "actian-ds-knowledge",
} as const;
